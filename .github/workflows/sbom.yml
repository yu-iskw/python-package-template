name: SBOM and dependency audit

on:
  pull_request:
    paths:
      - .github/workflows/sbom.yml
      - pyproject.toml
      - uv.lock
      - requirements.setup.txt
  push:
    branches:
      - main
    paths:
      - .github/workflows/sbom.yml
      - pyproject.toml
      - uv.lock
      - requirements.setup.txt
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sbom-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit-and-generate:
    name: Audit and generate SBOM
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install pinned uv
        run: |
          python -m pip install \
            --disable-pip-version-check \
            --requirement requirements.setup.txt

      - name: Verify lockfile consistency
        run: uv lock --check

      - name: Export all locked dependencies for auditing
        run: |
          mkdir -p artifacts
          uv export \
            --frozen \
            --all-groups \
            --no-emit-project \
            --format requirements-txt \
            --output-file artifacts/requirements-audit.txt

      - name: Audit locked dependencies
        run: |
          uvx --from pip-audit==2.10.1 pip-audit \
            --strict \
            --require-hashes \
            --requirement artifacts/requirements-audit.txt

      - name: Generate runtime CycloneDX SBOM
        run: |
          uv export \
            --frozen \
            --no-dev \
            --format cyclonedx1.5 \
            --output-file artifacts/sbom.cdx.json

      - name: Validate SBOM structure
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("artifacts/sbom.cdx.json")
          document = json.loads(path.read_text(encoding="utf-8"))

          assert document.get("bomFormat") == "CycloneDX"
          assert document.get("specVersion") == "1.5"
          assert isinstance(document.get("metadata"), dict)

          print(f"Validated {path}")
          PY

      - name: Upload SBOM evidence
        uses: actions/upload-artifact@v7
        with:
          name: cyclonedx-sbom
          path: |
            artifacts/sbom.cdx.json
            artifacts/requirements-audit.txt
          if-no-files-found: error
          retention-days: 30
